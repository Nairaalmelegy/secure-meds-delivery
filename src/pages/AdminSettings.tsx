import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

export default function AdminSettings() {
  // TODO: Add admin settings form and controls
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Admin Settings</CardTitle>
        </CardHeader>
        <CardContent>
          <div>Settings for the admin will appear here.</div>
        </CardContent>
      </Card>
    </div>
  );
}
